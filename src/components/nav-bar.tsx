import {
 AppBar, Typography
} from "@mui/material";

export function NavBar() {
  return (
    <AppBar position="sticky">
      <Typography variant="h1">
        Game Dev Testing Grounds
      </Typography>
    </AppBar>
  );
}
