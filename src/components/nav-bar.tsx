import {
 AppBar, Typography
} from "@mui/material";

export function NavBar() {
  return (
    <AppBar>
      <Typography variant="h1">
        Game Dev Testing Grounds
      </Typography>
    </AppBar>
  );
}
